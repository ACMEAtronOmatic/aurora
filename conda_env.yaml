#
# This file is autogenerated by pyproject2conda
# with the following command:
#
#     $ pyproject2conda yaml -f pyproject.toml -e dev -e myradar -o conda.yaml -p 3.10
#
# You should not manually edit this file.
# Instead edit the corresponding pyproject.toml file.
#

# NOTE: this environment may need to be used for the tile_server
# xESMF cannot be installed through pip

name: aurora_env
channels:
  - conda-forge
  - rapidsai
  - nvidia
  - defaults
dependencies:
  - python=3.11.9
  - boto3
  - build
  - cartopy
  - cdsapi
  - cfgrib
  - dask
  - einops
  - ghp-import
  - joblib
  - jupyter-book
  - matplotlib
  - mypy
  - netCDF4
  - numpy
  - opencv
  - pathlib
  - pre-commit
  - pytest
  - pytest-cov
  - ruff==0.4.1
  - scipy
  - timm==0.6.13
  - pytorch==2.6.0
  - wheel
  - xarray
  - xesmf
  - pip
  - pip:
    - huggingface-hub
